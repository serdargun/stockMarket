import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  portfolioName: {
    textAlign: 'center',
    paddingBottom: 10,
    fontSize: 18,
    fontWeight: '500',
  },
});
